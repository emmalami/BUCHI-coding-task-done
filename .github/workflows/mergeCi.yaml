name: Merge CI
on:
  push:
  pull_request:
   types: [opened, synchronize]
jobs:
  Test:
    runs-on: ${{ matrix.os }}
    strategy:
    matrix:
        os: [ windows-latest, macos-latest, ubuntu-latest ]
    steps:
     - uses: actions/checkout@v2
     - run: python main.py
